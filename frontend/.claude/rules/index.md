## 📜 Rules

1. **Architecture**: Never import NestJS-specific decorators inside `core/domain`.
2. **Naming**: Use PascalCase for components, kebab-case for files.
3. **Frontend Styling**: Strictly use Tailwind CSS. No external UI libraries (MUI, Shadcn, etc.).
